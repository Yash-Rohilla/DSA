#include <bits/stdc++.h>
using namespace std;
void ans(int arr[], int n, int x)
{
	sort(arr, arr + n, greater<int>());
	for (int i = 0; i < x; i++)
		cout << arr[i] << " ";
}
int main()
{
	int arr[100],n,x;
	cin>>n;
	for (int i = 0; i < n; i++) {
		cin>>arr[i];;
	}
	cin>>x;
	ans(arr, n, x);
	return 0;
}
